# One-time transfer of locally tested changes. No network, dynamic code or secrets.
# Input AND output SHA-256 are checked; only this fixed list of source files is written.
# This helper and its temporary integration workflow are removed after integration.
from pathlib import Path
import hashlib, json
FILES = json.loads(r'''{"server/config.mjs":{"before":"496ccc12a26c0defb09766aff1fdcbcf42958fb4d3b1de678aa192be3124d821","after":"2d61c3870de1aba34d28cc73d61229a32aff05dcc1cc7cb397e26f72d9f90036","edits":[[22,22,"  // Malik explicitly approved publishing the online build at this Pages origin.\n  // Keep the origin exact; other GitHub users and all wildcard origins stay denied.\n  if (production && !origins.includes('https://malikahed.github.io')) origins.push('https://malikahed.github.io');\n"]]},"server/index.mjs":{"before":"63d21478960c60e83a25492e0aa10e8e24876a79962cfa7bbe1b166711e7498b","after":"12c93a4ac211f63468f750b6bda827dcb1e43620cfdc5990be4b626447b1cacf","edits":[[1,1,"import { TimingWindow } from '../src/online/timing.js';\n"],[26,26,"  const stepTiming = new TimingWindow(), eventLoopTiming = new TimingWindow();\n  let timingReport = {};\n"],[44,45,"          rooms: lobby.rooms.size, capacity: config.maxRooms, beta: true, build: process.env.RENDER_GIT_COMMIT || process.env.GITHUB_SHA || 'local' }); return;\n"],[127,128,"        else if (message.type === 'ping') peer.send({ type: 'pong', nonce: message.nonce, rtt: peer.rtt ?? null, serverTiming: timingReport });\n"],[138,138,"    eventLoopTiming.add(Math.max(0, elapsed - 4));\n"],[143,144,"    while (accumulator >= 1000 / SIM_HZ && count++ < 24) { lobby.step(now, true); accumulator -= 1000 / SIM_HZ; metrics.ticks++; }\n    lobby.flushSnapshots();\n"],[145,146,"    const stepMs = performance.now() - start; stepTiming.add(stepMs);\n    metrics.maxStepMs = Math.max(metrics.maxStepMs, stepMs);\n"],[153,153,"      timingReport = { tickBatch: stepTiming.summary(), eventLoopDelay: eventLoopTiming.summary(), targetHz: SIM_HZ, skippedSnapshots: metrics.skippedSnapshots };\n"]]},"server/lobby.mjs":{"before":"4de9479f958d77a3478a521f18f81b07e87c828608a4b6de87a1f788f2aba248","after":"781853c625ccd4a2a1d4f5c2f2a854ec83acef25a686af3be177281e0de55e6e","edits":[[118,119,"  step(now, deferSnapshots = false) { for (const room of this.rooms.values()) room.step(now, deferSnapshots); }\n  flushSnapshots() { for (const room of this.rooms.values()) room.flushSnapshot(); }\n"]]},"server/room.mjs":{"before":"9aea1d7726295d5e035965f88ebc87d942d537e28c4212a8110e05cbf5e23ab5","after":"570b661c099cc7eac563364271861030af8990e51fd1cb39d0b843ffa3a1d912","edits":[[126,127,"  step(now = performance.now(), deferSnapshots = false) {\n"],[143,144,"    if (this.tick % (SIM_HZ / SNAPSHOT_HZ) === 0) {\n      if (deferSnapshots) this.snapshotDue = true;\n      else this.sendSnapshot();\n    }\n  }\n  flushSnapshot() {\n    if (!this.snapshotDue) return;\n    this.snapshotDue = false; this.sendSnapshot();\n"]]},"src/app/startup.js":{"before":"cafa8f3b8633c41ff610648e0ef091e0e45aace5fe2630e3fc663ab9e324314e","after":"8907cb965783ec95edea6e33c6774af77f84c94f2c7055d17c4d089a420b6dd4","edits":[[1166,1167,"  ((gt = new FrameScheduler(te.getContext(), wt, (W) => He.displayFrame(W), W => {\n      if (online?.active) { he(); online.update(W, ke, s, false); }\n    })),\n"]]},"src/online/interface.js":{"before":"b40ce6be78dbddb0221089cae362aa66d278cc989a4d60e5201bde309ec16f97","after":"aabae6afad7b6cad63ccd4bed92b5124bda88fa14a1b4bfbd7e23ab9862a1d6f","edits":[[26,27,"  let lastNetworkDisplay = 0, networkRtt = null, serverTiming = null;\n"],[80,80,"    find('[data-online=\"signin\"]').disabled = !config?.supabaseUrl || !config?.publishableKey;\n"],[139,140,"    profile = session.profile ?? null;\n"],[147,148,"      authSession = config.supabaseUrl && config.publishableKey ? await currentAccount(config) : null;\n      if (!authSession) profile = null;\n"],[149,150,"      setStatus(!config.serverUrl ? 'Online play is not configured for this build. Bots and Free Play still work offline.' : mode === 'ranked' && !authSession ? (!config.supabaseUrl || !config.publishableKey ? 'Sign in unavailable: the account provider is not configured. Ranked cannot start; Casual and offline modes remain available.' : friendly.sign_in_required) : 'Choose a playlist, then find a match. Matches start only when every human player is ready.');\n"],[208,209,"    else if (message.type === 'pong') { networkRtt = Math.max(0, Math.round(performance.now() - message.nonce)); serverTiming = message.serverTiming ?? null; }\n"],[328,329,"    update(now, controls, clock, render = true) {\n"],[331,332,"      prediction.update(now, document.hidden || !document.hasFocus() || !panel.hidden ? NEUTRAL : controlsArray(controls), (seq, values, metadata) => transport?.send({ type: 'input', matchId, seq, epoch: metadata.epoch, controls: values }), clock, usable, { render });\n"],[333,334,"      if (render && now - lastNetworkDisplay > 500) {\n"],[338,339,"            : `${reservation?.region ?? 'Server'} · ${networkRtt === null ? 'connected' : `${networkRtt} ms`} · jitter ${Math.round(stats.jitterMs)} ms${stats.frameTiming.p95Ms > 50 ? ' · slow rendering' : ''}${serverTiming?.eventLoopDelay?.p95Ms > 25 ? ' · server delay' : ''}`\n"],[347,348,"      correctionUnits: prediction?.error ?? 0, netcode: prediction?.metrics() ?? null, serverTiming, result: lastResult }; },\n"]]},"src/online/prediction.js":{"before":"d2984955fcacf39fd4e5df26b7de16900f00965af55a7491357033e2c4affbbc","after":"44d556a5012229ebba945e15bd0c497e3d0e48f8c02ae5d8a9f023fd1fccc5eb","edits":[[1,2,"import { controlsObject, NEUTRAL, reorderState, SIM_HZ, INPUT_HZ, CONTROL_KEYS } from './protocol.js';\nimport { TimingWindow } from './timing.js';\n"],[15,15,"        // Reuse bounded state storage instead of allocating a 510-float snapshot\n        // on every predicted AND replayed tick. One spare slot avoids aliasing.\n        this.packetPool = Array.from({ length: MAX_PENDING + 1 }, () => ({ seq: 0,\n            controls: [...NEUTRAL], object: controlsObject(NEUTRAL), ticks: 0, at: 0,\n            states: [simulation.state.slice(), simulation.state.slice()] }));\n        this.packetCursor = 0; this.frameTiming = new TimingWindow(); this.updateTiming = new TimingWindow();\n        this.beforeScratch = simulation.state.slice();\n        this.ballScratch = simulation.state.slice();\n        this.frozenScratch = new Float32Array(STRIDE);\n"],[19,20,"        this.last = null; this.lastPresentation = null;\n"],[103,104,"        const acknowledged = this.pending.find(p => p.seq === meta.seq);\n        const historical = acknowledged && meta.ticks > 0 && meta.ticks <= acknowledged.ticks\n            ? acknowledged.states[meta.ticks - 1] : null;\n"],[121,122,"            const before = this.beforeScratch; before.set(this.sim.state);\n"],[133,134,"                    this.sim.setControls(0, packet.object);\n"],[137,138,"                        packet.states[t].set(this.sim.state);\n"],[156,157,"    update(now, controls, send, clock, connected, { render = true } = {}) {\n"],[161,161,"        const updateStarted = performance.now();\n"],[171,172,"                    packet = this.packetPool[this.packetCursor++ % this.packetPool.length];\n                    packet.seq = ++this.seq; packet.ticks = 0; packet.at = now;\n                    for (let i = 0; i < 8; i++) packet.controls[i] = controls[i];\n                    // Keep the same object consumed by PhysicsSimulation.setControls.\n                    for (let i = 0; i < 8; i++) packet.object[CONTROL_KEYS[i]] = i < 5 ? controls[i] : !!controls[i];\n"],[177,178,"                this.sim.setControls(0, packet.object);\n"],[180,181,"                packet.states[packet.ticks++].set(this.sim.state);\n"],[189,190,"        if (!render) {\n            this.updateTiming.add(performance.now() - updateStarted);\n            return;\n        }\n        const renderElapsed = this.lastPresentation === null ? 0 : Math.max(0, now - this.lastPresentation);\n        this.lastPresentation = now;\n        if (renderElapsed) this.frameTiming.add(renderElapsed);\n        const frozenLocal = !playing && this.latest.match.phase === 'playing';\n        if (frozenLocal) this.frozenScratch.set(this.rendered.subarray(L.CARS, L.CARS + STRIDE));\n"],[197,198,"            this.localVisual.apply(this.rendered, renderElapsed / 1000);\n"],[202,204,"            this.ballWeight += (target - this.ballWeight) * (1 - Math.exp(-Math.min(renderElapsed, 100) / 65));\n            const ball = this.ballScratch; ball.set(this.curr);\n"],[205,206,"            this.ballVisual.apply(ball, renderElapsed / 1000);\n"],[211,212,"            this.rendered.set(this.frozenScratch, L.CARS);\n"],[226,226,"        this.updateTiming.add(performance.now() - updateStarted);\n"],[236,238,"    metrics() { return { protocol: 2, frameTiming: this.frameTiming.summary(), predictionWork: this.updateTiming.summary(), historyBytes: this.packetPool.length * 2 * this.curr.byteLength, playbackRebases: this.timeline.rebases, playbackAgeMs: this.timeline.ageMs, resynchronizations: this.resynchronizations, predictionPaused: !this.wasPlaying, simulatedTicks: this.simulatedTicks, droppedTicks: this.droppedTicks, corrections: this.corrections, skippedRestores: this.skippedRestores, correctionUnits: this.error, visualOffsetUnits: Math.hypot(...this.localVisual.position), hardSnaps: this.localVisual.hardSnaps, jitterMs: this.timeline.jitterMs, interpolationMs: this.timeline.delayMs, interpolationUnderruns: this.timeline.underflows, inputQueueTicks: this.averageQueue, inputTimeScale: this.rate, maxReplayTicks: this.maxReplayTicks }; }\n    dispose() { this.sim.module._free(this.pointer); this.pending.length = 0; this.timeline.clear(); this.latest = this.queuedSnapshot = null; this.packetPool.length = 0; }\n"]]},"src/online/protocol.js":{"before":"c043a5d1c4549a2abbe3aa344dba2fc501c78445362a4d8615e96cb693dbc59a","after":"5b1673328b913c3eeeb2e24b435fe4d590624d421a7458ca3f709663a34c6047","edits":[[85,85,"    if ([24, 28, 32].some(at => !Number.isFinite(view.getFloat32(at, true)) || view.getFloat32(at, true) < 0)\n        || view.getUint8(17) > 1 || view.getUint8(18) > 2 || view.getUint8(19) > 2)\n        throw new Error('invalid_snapshot');\n"]]},"src/online/snapshot-buffer.js":{"before":"4010dc302fb7ce8f8e77e3d7f172474d48154d35560ceb9f7458bee87ec237bd","after":"6eaeb7ca2f207e0a0363513097ddf7fb3ed30f2f190b4373d3d7ffc4fade6f3a","edits":[[5,6,"    constructor() { this.samples = []; this.epoch = -1; this.playhead = null; this.lastRender = null; this.jitterMs = 0; this.delayMs = 100; this.underflows = 0; this.rejected = 0; this.rebases = 0; this.ageMs = 0; }\n"],[8,9,"        if (last && (snapshot.epoch < this.epoch || snapshot.epoch === this.epoch && snapshot.tick <= last.tick)) {\n"],[38,38,"        else if (desired - this.playhead > Math.max(250, this.delayMs + 100)\n            || this.playhead < this.samples[0].time - this.delayMs) {\n            // Suspension/alt-tab recovery is an explicit rebase, not a hidden\n            // many-second slowdown. Ordinary jitter still uses monotonic slew.\n            this.playhead = Math.min(newest.time, desired); this.rebases++;\n        }\n"],[47,47,"        this.ageMs = Math.max(0, newest.time - this.playhead);\n"]]},"src/online/transport.js":{"before":"710cdcce6e13be00c63717d2a7398a3578bef82cf5343b8a1f16da44d7c279e4","after":"f96a2d6761cffe97e5917fd1f6b9cb10cde542545d7870eb542d36adf10af681","edits":[[57,58,"    if (message.type === 'input' && this.socket.bufferedAmount > 16384) { this.socket.close(4013, 'input_congestion'); return false; }\n"]]},"src/rendering/frame-scheduler.js":{"before":"0c77f08dc2a2a7cc11ed25a9f289441369b9bf486903a005cfea9b98c6de58a8","after":"f144b546b28cf843d995c3be060fd0a10da6a312323dcddfd8e037a900a2d9fa","edits":[[4,5,"  constructor(e, t, n, maintain = null) {\n    this.maintain = maintain; this.lastMaintenance = 0;\n"],[62,62,"      }\n      if (this.pending.length >= 2 && this.maintain && e - this.lastMaintenance >= 1000 / 120) {\n        // GPU completion must not starve online input/physics. This optional\n        // callback does not draw, change a preset, or run offline networking.\n        this.lastMaintenance = e; this.maintain(e);\n"]]},"tests/netcode/harness.mjs":{"before":"c2b3bdb256a0c177bbb2f6624a7c3dd288e993dfcbb6d36d8a13a826bc95e30c","after":"9af8bb1665e9f7b99f208e5eef7437dd0eab7fb69d33f5d0b45b137f18298681","edits":[[10,11,"export async function scenario(Predictor, { name = 'drive', fps = 60, rtt = 80, jitter = 0, seconds = 12, size = 1, seed = 1717, streamFactory, input, stall = false, stallDuration = 150, downLoss = 0, opponents, setup, maintenanceHz = 0 } = {}) {\n"],[22,23,"    let nextRemote = 0, remoteSeq = 0, nextMaintenance = maintenanceHz ? 0 : Infinity;\n"],[35,36,"            now = Math.min(nextTick, nextFrame, nextMaintenance, opponents ? nextRemote : Infinity, up[0]?.at ?? Infinity, down[0]?.at ?? Infinity);\n"],[88,88,"            if (nextMaintenance <= now + 1e-6) {\n                prediction.update(now, getControls(now), send, clock, true, { render: false });\n                nextMaintenance += 1000 / maintenanceHz;\n            }\n"]]},"tests/online/transport.test.mjs":{"before":"93d1a88f3387a8a90a8566618ed89245b3dedff266d693f888e0750061d736b0","after":"e893b93c7035b4deea58a75515bab0104c25f0b676f8e2b8212b38b16cd7ae86","edits":[[48,48,"\ntest('browser congestion: close code is script-legal and enters authenticated reconnect without throwing', () => {\n  const f=fixture();\n  try {\n    f.transport.connect(); const socket=f.sockets[0]; socket.message({type:'reserved',graceMs:30000});\n    const originalClose=socket.close.bind(socket); let usedCode;\n    socket.close=code=>{ if(code!==1000 && (code<3000 || code>4999)) throw new DOMException('Invalid close code','InvalidAccessError'); usedCode=code; originalClose(code); };\n    socket.bufferedAmount=20000;\n    assert.doesNotThrow(()=>assert.equal(f.transport.send({type:'input',seq:1}),false));\n    assert.equal(usedCode,4013); assert.equal(f.states.at(-1),'reconnecting');\n  } finally { f.restore(); }\n});\n"]]},"tools/build.mjs":{"before":"03a0a0c62a39e339dc4de2bdc2ff54f011535ac4544bde3f50e3d1e28ddd5130","after":"c3c88429277ddc698ad1a08cecfb374d5d1922db33d3899f7175385f88c5fc0f","edits":[[26,26,"const release = process.env.GITHUB_SHA || process.env.RENDER_GIT_COMMIT;\nif (release && /^[a-f0-9]{40}$/.test(release)) {\n  html = html.replace('/app/game.js', `/app/game.js?v=${release}`).replace('/app/game.css', `/app/game.css?v=${release}`);\n}\n"]]},"tools/online-config.mjs":{"before":"24928daceb8f048eb32e74af345020a4dfd634ed0390c10b6c0803e5b8ffd9c4","after":"41658bc0fe2f90268a1ad8385c8655fae70c8e8d726b67fc9ea051e55374a840","edits":[[12,13,"  return { serverUrl, supabaseUrl, publishableKey, protocol: 2, build: env.GITHUB_SHA || env.RENDER_GIT_COMMIT || 'local' };\n"]]},"src/online/timing.js":{"before":null,"after":"aaaa5837bf62cc149c8ba691729d7e2700b4f93dae437541517374180bdc8081","content":"// Bounded telemetry: record cheaply, summarize only when the HUD/diagnostic asks.\n// These are measurements, not a competitive-readiness certification.\nexport class TimingWindow {\n  constructor(capacity = 240) {\n    if (!Number.isInteger(capacity) || capacity < 1 || capacity > 4096) throw Error('Invalid timing capacity');\n    this.values = new Float32Array(capacity); this.cursor = 0; this.count = 0; this.total = 0; this.maximum = 0;\n  }\n  add(value) {\n    if (!Number.isFinite(value) || value < 0) return;\n    this.values[this.cursor++ % this.values.length] = value;\n    this.count = Math.min(this.values.length, this.count + 1); this.total++; this.maximum = Math.max(this.maximum, value);\n  }\n  summary() {\n    const sorted = Array.from(this.values.subarray(0, this.count)).sort((a,b)=>a-b);\n    const at = q => sorted[Math.max(0, Math.ceil(sorted.length*q)-1)] ?? 0;\n    return { samples: this.count, total: this.total, p50Ms: at(.5), p95Ms: at(.95), p99Ms: at(.99), maxMs: this.maximum };\n  }\n}\n"},"tests/online/stability.test.mjs":{"before":null,"after":"8d5bbf144cb643123b6ccb9607da7915b87c63e2a491dd0208a74b9594554b15","content":"import test from 'node:test';\nimport assert from 'node:assert/strict';\nimport { SnapshotBuffer } from '../../src/online/snapshot-buffer.js';\nimport { TimingWindow } from '../../src/online/timing.js';\nimport { Prediction } from '../../src/online/prediction.js';\nimport { InputStream } from '../../server/input-stream.mjs';\nimport { Room } from '../../server/room.mjs';\nimport { NativeArena } from '../../server/native.mjs';\nimport { STATE_LAYOUT as L } from '../../src/physics/state-layout.js';\nimport { STATE_SIZE, encodeSnapshot, decodeSnapshot, controlsArray } from '../../src/online/protocol.js';\nimport { scenario } from '../netcode/harness.mjs';\nimport { opponent } from '../netcode/fixtures.mjs';\nimport { mkdir, writeFile } from 'node:fs/promises';\nconst streamFactory = () => new InputStream();\nfunction frame(tick) { const state=new Float32Array(STATE_SIZE); state[L.NUM_CARS]=2; state[L.NUM_PADS]=34; state[L.CARS]=tick; return state; }\n\ntest('suspension recovery: remote playback does not stay seconds behind or accept stale epochs', () => {\n  const buffer = new SnapshotBuffer(), out=frame(0);\n  for (let tick=0; tick<=120; tick+=6) { buffer.add({tick,epoch:1},frame(tick),tick*1000/120); buffer.render(tick*1000/120,out); }\n  const prior=buffer.playhead;\n  // Rendering suspends for three seconds while networking continues.\n  for (let tick=126; tick<=480; tick+=6) buffer.add({tick,epoch:1},frame(tick),tick*1000/120);\n  buffer.render(4000,out);\n  assert(buffer.playhead >= 3850, `old playhead ${prior}, resumed ${buffer.playhead}`);\n  assert(buffer.ageMs <= 150); assert.equal(buffer.rebases,1);\n  buffer.add({tick:1,epoch:0},frame(1),4010);\n  assert.equal(buffer.epoch,1); assert.equal(buffer.rejected,1);\n});\n\ntest('snapshot parser rejects invalid clock/winner metadata rather than rendering NaNs', () => {\n  const match={phase:'playing',overtime:false,winner:null,scorer:null,blueScore:0,orangeScore:0,remainingSeconds:300,overtimeSeconds:0,countdown:0};\n  for (const at of [24,28,32]) {\n    const data=encodeSnapshot({tick:1,state:frame(1),match,acknowledgements:[0,0]}); new DataView(data).setFloat32(at,NaN,true);\n    assert.throws(()=>decodeSnapshot(data), /invalid_snapshot/);\n  }\n});\n\ntest('timing windows remain bounded and report stutters instead of hiding them', () => {\n  const timing=new TimingWindow(10);\n  for(let i=0;i<1000;i++) timing.add(16);\n  timing.add(250); timing.add(NaN); timing.add(-1);\n  assert.equal(timing.values.length,10); assert.equal(timing.summary().total,1001);\n  assert.equal(timing.summary().p95Ms,250); assert.equal(timing.summary().p50Ms,16);\n});\n\ntest('server catch-up preserves every physics tick but emits only the newest scheduled snapshot', async () => {\n  const players=[0,1].map(team=>({id:`fixture:${team}`,team,visual:'fennec'}));\n  const sent=[];\n  const room=new Room({players,size:1,mode:'casual',region:'test',peerFor:()=>({send(){},sendBinary:b=>sent.push(decodeSnapshot(b))}),onDispose(){}});\n  room.arena=await NativeArena.create(players); room.active=true; room.match.state.phase='playing';\n  try {\n    for(let i=0;i<24;i++) room.step(i*1000/120,true);\n    assert.equal(room.tick,24); assert.equal(sent.length,0);\n    room.flushSnapshot(); assert.equal(sent.length,2); assert(sent.every(s=>s.tick===24));\n    room.flushSnapshot(); assert.equal(sent.length,2);\n  } finally { room.arena.dispose(); }\n});\n\ntest('two-minute six-car native soak: bounded reusable state storage and replay with variable inputs', {timeout:45000}, async () => {\n  let pooled;\n  class Audited extends Prediction {\n    constructor(...args) { super(...args); pooled=new Set(this.packetPool.flatMap(p=>p.states)); }\n    update(...args) { super.update(...args);\n      assert(this.pending.every(p=>p.states.every(s=>pooled.has(s))));\n      assert.equal(new Set(this.pending).size,this.pending.length,'history buffers cannot alias live commands');\n    }\n  }\n  const r=await scenario(Audited,{name:'120-second-6car-soak',seconds:120,size:3,rtt:80,jitter:20,opponents:opponent,input:t=>opponent(t,0),streamFactory});\n  assert.equal(r.serverTicks,14400); assert(r.queuedMax<=60 && r.replayMax<=120);\n  assert.equal(r.diagnostics.historyBytes,248880); assert(r.serverInput.queued <= 32);\n  delete r.trace;\n  await mkdir('evidence/netcode',{recursive:true}); await writeFile('evidence/netcode/long-session.json',JSON.stringify(r,null,2));\n});\n\ntest('GPU-limited rendering does not halve input/simulation rate; telemetry still reports the slow frames', async () => {\n  const r=await scenario(Prediction,{name:'gpu-blocked-5fps',fps:5,maintenanceHz:120,seconds:6,streamFactory});\n  assert(r.inputs>=345 && r.inputs<=365,`commands=${r.inputs}`);\n  assert(r.diagnostics.simulatedTicks > 700);\n  assert(r.diagnostics.frameTiming.p95Ms >= 190,'must not mislabel maintenance as rendered frames');\n  assert.equal(r.diagnostics.droppedTicks,0);\n});\n\ntest('approved production Pages origin is exact, never a wildcard GitHub origin', async()=>{\n  const {configuration}=await import('../../server/config.mjs');\n  const config=configuration({NODE_ENV:'production',ALLOWED_ORIGINS:'https://rocket-arena-online-preview.onrender.com'});\n  assert(config.origins.includes('https://malikahed.github.io'));\n  assert(!config.origins.includes('https://attacker.github.io'));\n  assert(!config.origins.includes('*'));\n});\n"},"tests/online/gpu-maintenance.test.mjs":{"before":null,"after":"e0df95d8799cc150022b9ac9c490683dc87e1d0b4f8ef2d837cdf24aa573fcf7","content":"import test from 'node:test';\nimport assert from 'node:assert/strict';\nimport { FrameScheduler } from '../../src/rendering/frame-scheduler.js';\n\ntest('GPU fences throttle rendering but allow explicitly registered maintenance',()=>{\n  const old={document:globalThis.document,MessageChannel:globalThis.MessageChannel};\n  globalThis.document={hidden:false,addEventListener(){},removeEventListener(){}};\n  globalThis.MessageChannel=class {constructor(){this.port1={close(){}};this.port2={close(){},postMessage(){}};}};\n  const gl={TIMEOUT_EXPIRED:1,clientWaitSync:()=>1,isContextLost:()=>false,deleteSync(){},canvas:{addEventListener(){},removeEventListener(){}}};\n  let draws=0,pumps=0;\n  const scheduler=new FrameScheduler(gl,()=>draws++,()=>{},()=>pumps++);\n  try {\n    scheduler.schedule=()=>{}; scheduler.running=true; scheduler.lastMaintenance=-1000;\n    scheduler.pending.push({},{}); scheduler.tick();\n    assert.equal(draws,0);assert.equal(pumps,1);assert.equal(scheduler.pending.length,2);\n    scheduler.tick(); assert.equal(pumps,1,'maintenance itself is rate-bounded');\n    globalThis.document.hidden=true;scheduler.lastMaintenance=-1000;scheduler.tick();assert.equal(pumps,1,'hidden tab must not keep controlling its car');\n  } finally {scheduler.dispose(); Object.assign(globalThis,old);}\n});\n"}}''')

def apply(root):
    outputs = {}
    for name, spec in FILES.items():
        path = root / name
        before = path.read_bytes() if path.exists() else None
        assert (hashlib.sha256(before).hexdigest() if before is not None else None) == spec['before'], f'Concurrent change: {name}'
        if before is None:
            after = spec['content'].encode()
        else:
            lines = before.decode().splitlines(keepends=True)
            for start, end, replacement in reversed(spec['edits']):
                lines[start:end] = replacement.splitlines(keepends=True)
            after = ''.join(lines).encode()
        assert hashlib.sha256(after).hexdigest() == spec['after'], f'Transfer mismatch: {name}'
        outputs[path] = after
    for path, content in outputs.items():
        path.parent.mkdir(parents=True, exist_ok=True)
        path.write_bytes(content)
    print('Applied exact tested contents for', len(outputs), 'source files')

if __name__ == '__main__':
    apply(Path('.'))
