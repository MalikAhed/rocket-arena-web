// Finite reviewed source integration; removed after CI verifies and commits it.
import fs from 'node:fs';
if (process.env.GITHUB_REF !== 'refs/heads/online-play') throw Error('Wrong branch');
const touched = new Set();
function edit(path, before, after) {
  const text = fs.readFileSync(path, 'utf8');
  if (!text.includes(before)) throw Error(`Expected source changed: ${path}`);
  fs.writeFileSync(path, text.replace(before, after)); touched.add(path);
}
edit("server/store.mjs","  async init() {","  async init({ waitForLeaseMs = 0 } = {}) {");
edit("server/store.mjs","    const lock = await this.lease.query(\"SELECT pg_try_advisory_lock(hashtext('rocket-arena-online-v1')) AS locked\");\n    if (!lock.rows[0].locked) { this.lease.release(); this.lease = null; throw new Error('Another authoritative server holds the database lease'); }","    const deadline = performance.now() + waitForLeaseMs;\n    while (true) {\n      const lock = await this.lease.query(\"SELECT pg_try_advisory_lock(hashtext('rocket-arena-online-v1')) AS locked\");\n      if (lock.rows[0].locked) break;\n      if (this.closeRequested || performance.now() >= deadline) {\n        this.lease.release(); this.lease = null;\n        throw new Error('Another authoritative server holds the database lease');\n      }\n      await new Promise(resolve => setTimeout(resolve, 500));\n    }");
edit("server/index.mjs","  if (store && !dependencies.store) await store.init();\n","");
edit("server/index.mjs","  const rates = new BoundedRates(), peers = new Set();","  lobby.initializing = !!store && !dependencies.store;\n  const rates = new BoundedRates(), peers = new Set();");
edit("server/index.mjs","const ready = !lobby.stopping && (!store || store.healthy);","const ready = !lobby.stopping && !lobby.initializing && (!store || store.healthy);");
edit("server/index.mjs","status: lobby.stopping ? 'draining' : 'ok'","status: lobby.stopping ? 'draining' : lobby.initializing ? 'warming' : 'ok'");
edit("server/index.mjs","      if (!originAllowed(origin)) throw new PublicError('origin_forbidden');","      if (!originAllowed(origin)) throw new PublicError('origin_forbidden');\n      if (lobby.initializing) throw new PublicError('server_warming', 'The replacement server is warming up. No new matches can start yet.');");
edit("server/index.mjs","safe.code === 'server_unavailable' || safe.code.endsWith('unavailable')","safe.code === 'server_unavailable' || safe.code === 'server_warming' || safe.code.endsWith('unavailable')");
edit("server/index.mjs","|| lobby.stopping || peers.size","|| lobby.stopping || lobby.initializing || peers.size");
edit("server/index.mjs","  return { server, wss, lobby, sessions, store, metrics, port: server.address().port,\n    async close() {\n      clearInterval(timer); clearInterval(readiness); lobby.shutdown();\n      await Promise.all([...lobby.rooms.values()].map(room => room.persist()));","  // Liveness binds before the exclusive database lease is acquired. Render\n  // can route a replacement and terminate the old process; readiness remains\n  // false throughout this deliberate beta maintenance window. No split brain.\n  const initialization = lobby.initializing\n    ? store.init({ waitForLeaseMs: 120000 }).then(() => { lobby.initializing = false; return true; })\n      .catch(() => { lobby.initializing = false; lobby.shutdown('database_startup_failed'); return false; })\n    : Promise.resolve(true);\n  return { server, wss, lobby, sessions, store, metrics, initialization, port: server.address().port,\n    async close() {\n      clearInterval(timer); clearInterval(readiness);\n      if (store && !dependencies.store) store.closeRequested = true;\n      await initialization;\n      lobby.shutdown();\n      const pending = [...lobby.rooms.values()];\n      await Promise.all(pending.map(room => room.persist()));\n      const deadline = performance.now() + 6000;\n      while (pending.some(room => room.busy) && performance.now() < deadline) await new Promise(resolve => setTimeout(resolve, 20));");
edit("server/index.mjs","    await app.close(); process.exit(0);\n  });\n}","    await app.close(); process.exit(0);\n  });\n  if (!await app.initialization) { await app.close(); process.exitCode = 1; }\n}");
edit("server/lobby.mjs","    if (this.stopping) throw new PublicError('server_unavailable');","    if (this.stopping || this.initializing) throw new PublicError('server_unavailable');");
edit("server/lobby.mjs","    if (this.stopping) return;","    if (this.stopping || this.initializing) return;");
edit("server/config.mjs","  const rating = validateRatingConfig","  if (env.DATABASE_URL) {\n    const url = new URL(env.DATABASE_URL);\n    if (![\"postgres:\", \"postgresql:\"].includes(url.protocol)) throw new Error('DATABASE_URL must use PostgreSQL');\n    if ([...url.searchParams.keys()].some(key => key.toLowerCase().startsWith('ssl'))) throw new Error('Configure TLS with DATABASE_SSL/DATABASE_CA_FILE, not URL options that override certificate validation');\n    if (url.port === '6543') throw new Error('Use a direct or SESSION pooler connection, not transaction mode on port 6543');\n  }\n  const rating = validateRatingConfig");
edit("src/app/startup.js","n.setGoalExplosionEnabled(a.state.mode === \"match\" || !V.disableGoalReset);","n.setGoalExplosionEnabled(!online?.active && (a.state.mode === \"match\" || !V.disableGoalReset));");
edit("src/online/transport.js","this.retryStart = 0; this.retry = 0;","this.retryStart = 0; this.retry = 0; this.graceMs = 30000;");
edit("src/online/transport.js","if (message.type === 'reserved') this.inMatch = true;","if (message.type === 'reserved') { this.inMatch = true; this.graceMs = message.graceMs; }");
edit("src/online/transport.js","performance.now() - this.retryStart < 30000","performance.now() - this.retryStart < this.graceMs");
edit("tools/render-online-art.mjs","Z m87 119 29-5-1 9-29 6Z","Z M87 119l29-5-1 9-29 6Z");
edit("tests/online/browser.mjs","    const page = await context.newPage(); pages.push(page);","    const page = await context.newPage(); page.setDefaultTimeout(25000); pages.push(page);\n    console.log('Opening independent browser:', name);");
edit("tests/online/browser.mjs","  for (const size of [1, 2, 3]) {","  for (const size of [1, 2, 3]) {\n    console.log('Testing playlist', size);");
edit("index.html","    <link rel=\"stylesheet\" href=\"/src/styles/online.css\" />\n","");
edit("index.html","  </head>","    <link rel=\"stylesheet\" href=\"/src/styles/online.css\" />\n  </head>");
edit("tools/serve.mjs","const hostname = \"127.0.0.1\";","const hostname = process.env.HOST || \"127.0.0.1\";");
edit("src/styles/online.css","right:16px;bottom:70px;","right:16px;top:155px;bottom:auto;");
edit("src/styles/online.css","[data-forfeit] { bottom:110px; }","[data-forfeit] { top:110px;bottom:auto; }");
edit("src/ui/home-screen.js","/assets/online/illustrations.svg#ranked","/assets/online/ranked.webp");
edit("src/ui/home-screen.js","/assets/online/illustrations.svg#bots","/assets/online/bots.webp");
edit("src/online/interface.js","/assets/online/illustrations.svg#team-${n}","/assets/online/team-${n}.webp");
edit("tests/online/browser.mjs","await primary.page.locator('.arcade-home-button').click();","await primary.page.keyboard.press('Escape'); await primary.page.locator('[data-pause=\"home\"]').click();");
edit("src/online/interface.js","      try { if (session) await api('/logout', { body: {}, token: session.token }); const client = await accountClient(config); await client.auth.signOut(); }\n      catch (error) { setStatus(error.message); }\n      finally { leave(); session = authSession = profile = null; storage.set('session', null); input.disabled = false; controls(); }\n","      try { if (session) await api('/logout', { body: {}, token: session.token }); } catch {}\n      try { const client = await accountClient(config); const { error } = await client.auth.signOut({ scope: 'local' }); if (error) throw error; }\n      catch (error) { setStatus(error.message); }\n      finally { leave(); session = authSession = profile = null; storage.set('session', null); input.disabled = false; controls(); }\n");
edit("tests/online/database.test.mjs","      await store.close();\n      const restarted = new Store({ ...options, serverId: randomUUID() });\n      try {\n        await restarted.init();\n        assert.equal((await restarted.pool.query('SELECT status FROM arena.matches WHERE id=$1', [room.id])).rows[0].status, 'cancelled');\n        assert.equal((await restarted.getProfile(players[0].accountId)).progress[0].games, 2);\n      } finally { await restarted.close(); }","      const app = await createGameServer(configuration({ PORT: '0', DATABASE_URL: options.connectionString, SEASON_ID: options.season }));\n      try {\n        const origin = `http://127.0.0.1:${app.port}`;\n        assert.equal((await fetch(origin + '/healthz')).status, 200);\n        assert.equal((await fetch(origin + '/readyz')).status, 503);\n        assert.equal((await (await fetch(origin + '/healthz')).json()).status, 'warming');\n        const admission = await fetch(origin + '/session', { method: 'POST', headers: {Origin: 'http://127.0.0.1:4173', 'Content-Type': 'application/json'}, body: JSON.stringify({name:'Waiting Player'}) });\n        assert.equal(admission.status, 503);\n        await store.close();\n        assert.equal(await app.initialization, true);\n        assert.equal((await fetch(origin + '/readyz')).status, 200);\n        assert.equal((await app.store.pool.query('SELECT status FROM arena.matches WHERE id=$1', [room.id])).rows[0].status, 'cancelled');\n        assert.equal((await app.store.getProfile(players[0].accountId)).progress[0].games, 2);\n      } finally { await app.close(); }");
edit("src/online/interface.js","    if (message.type === 'connected' && searching && !queueSent) {","    if (message.type === 'connected' && !searching && storage.get('match')) {\n      storage.set('match', null); transport.inMatch = false;\n      setStatus('The previous match is no longer available on this server. Interrupted server matches are no-contest; completed results remain in account history.');\n      show(); controls();\n    } else if (message.type === 'connected' && searching && !queueSent) {");
edit("src/online/interface.js","rating.games < 10 ? `${rating.games}/10 placements`","rating.games < rating.rank.placementTotal ? `${rating.games}/${rating.rank.placementTotal} placements`");
edit("tests/online/database.test.mjs","import { Sessions } from '../../server/auth.mjs';","import { Sessions } from '../../server/auth.mjs';\nimport { createGameServer } from '../../server/index.mjs';\nimport { configuration } from '../../server/config.mjs';");
edit("tests/online/browser.mjs","  await primary.page.screenshot({ path: `${out}/mode-menu.png` });\n","  const withinViewport = await primary.page.locator('.arena-mode').evaluateAll(items => items.every(item => {\n    const r = item.getBoundingClientRect(), label = item.querySelector('small').getBoundingClientRect();\n    return r.top >= 0 && r.bottom <= innerHeight && label.bottom <= innerHeight;\n  }));\n  assert(withinViewport, 'All four mode cards and their labels must fit the screen');\n  await primary.page.screenshot({ path: `${out}/mode-menu.png` });\n  await primary.page.setViewportSize({width: 390, height: 844});\n  await primary.page.screenshot({ path: `${out}/mode-menu-mobile.png` });\n  assert(await primary.page.locator('.arena-mode').evaluateAll(items => items.every(item => item.getBoundingClientRect().bottom <= innerHeight)));\n  await primary.page.setViewportSize({width: 1280, height: 720});\n");
fs.writeFileSync(process.env.RUNNER_TEMP + '/online-edited.json', JSON.stringify([...touched]));
